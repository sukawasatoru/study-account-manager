plugins {
    alias libs.plugins.android.library
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

android {
    namespace 'com.example.study.accountmanager.contract'
}
